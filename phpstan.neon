includes:
    - phpstan-baseline.neon

parameters:
    level: max
    paths:
        - src
        - yii
    tmpDir: runtime/cache/phpstan
    excludePaths:
        analyse:
            - src/autoload.php
